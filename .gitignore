# Variáveis de ambiente (segredos) — nunca versionar
.env
.env.*
!.env.example

# Dados locais / volumes
data/
*.local

# Log de execução do pipeline (gerado localmente)
pipeline_run.log

# Driver Starburst baixado/montado localmente — ele é obtido no build da
# imagem do Metabase (docker/metabase/Dockerfile), não deve ser versionado.
docker/metabase/plugins/
*.metabase-driver.jar

# Saídas geradas pelo Papermill / notebooks
notebooks/output/
notebooks/runs/
.ipynb_checkpoints/

# Build do MkDocs
site/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Sistema operacional / editores
.DS_Store
Thumbs.db
.idea/
.vscode/
